import { Box, Typography } from "@mui/material";

import styles from "./ChartsPage.module.scss";

export const ChartsPage = () => {
  return (
    <Box className={styles.container}>
      <Typography variant="h1">Charts Page</Typography>
    </Box>
  );
};
